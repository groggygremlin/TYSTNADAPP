# Paid product text. NEVER commit. Publishing this = publishing the booklets.
canon/

# Local test tooling only
node_modules/
package.json
package-lock.json
smoke.js

# Mac noise
.DS_Store

# Local Claude Code setup: personal settings and the dev-server launch config.
# The whole directory, so nothing new dropped in here reaches a public repo.
.claude/
