# Paid product text. NEVER commit. Publishing this = publishing the booklets.
canon/

# Local test tooling. smoke.js IS committed (2026-07-19): it holds the assertions that
# enforce design rulings, and it needs to survive this laptop. Its one dev dependency is
# documented in its header rather than a manifest, so the repo stays dependency-file-free.
node_modules/
package.json
package-lock.json

# Mac noise
.DS_Store

# Local Claude Code setup: personal settings and the dev-server launch config.
# The whole directory, so nothing new dropped in here reaches a public repo.
.claude/
